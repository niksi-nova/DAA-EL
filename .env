# Single source of truth for the backend port. Both the Flask API (app.py)
# and the React frontend (vite.config.js / api.js) read this same value, so
# changing the port only ever requires editing it here.
PORT=5002
